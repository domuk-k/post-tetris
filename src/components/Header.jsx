import { Box, Heading } from '@chakra-ui/react';

const Header = () => {
  return (
    <Box as="header" textAlign="center">
      <Heading>TETRIS</Heading>
    </Box>
  );
};

export default Header;
